const db = require("../db/dbConnection");
const { handleError, handleSuccess } = require("../utils/response");

class SavedRouteController {
  getAll = async (req, res) => {};
}

module.exports = new SavedRouteController();
